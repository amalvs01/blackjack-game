let cards = [7, 2, 3]

//create for a loop tht logs out  all the cards in the array
//use cards.length to specify how long the loop should run

for(let i = 0; i < cards.length; i ++){//(++ / += 1)are same
 console.log(cards[i])
}